# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: objective-c
# cache: cocoapods
# podfile: Example/Podfile
before_install:
#- gem install cocoapods # Since Travis is not always on latest version
- pod install --project-directory=Example
script:
#- set -o pipefail && xcodebuild test -workspace Example/IOSQlite.xcworkspace -scheme IOSQlite-Example -sdk iphonesimulator 8.1 | xcpretty
#- set -o pipefail && xcodebuild test -workspace Example/IOSQlite.xcworkspace -scheme IOSQlite-Example -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty
- set -o pipefail && xcodebuild test -workspace Example/IOSQlite.xcworkspace -scheme IOSQlite-Example -destination 'platform=iOS Simulator,name=iPhone 6' | xcpretty
- pod lib lint --only-errors
